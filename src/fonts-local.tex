\documentclass[11pt]{article}

\usepackage{fontspec}
\setsansfont[
    % Fontspec does not accept spaces in local paths, so we have to rename the font files
    Path=fonts/otf-without-spaces/,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Regular-Italic
]{ProximaNova}
\setmonofont[
    Path=fonts/ttf/,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic
]{UbuntuMono}

\newfontfamily\warwickfont[
    Path=fonts/ttf/,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
    Scale=MatchLowercase
]{AvenirNext}

% Compile using XeLaTeX or LuaLaTeX
% Document is like fonts.tex but using fonts from a local path
\begin{document}
    \textbf{Test} \textit{loading} \verb|fonts|.
\end{document}